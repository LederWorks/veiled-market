{#- CLAUDE.md.j2 — v1.0.0 -#}
{#- Variables:
     project_name (string, required) — project display name
     project_description (string, required) — one-line project description
     has_instructions_dir (bool, optional) — whether .github/instructions/ exists
     inventory_items (list[{path, purpose, status}], optional) — component inventory rows
     prerequisites (string, optional) — build prerequisites prose
     build_commands (list[{command, description}], optional) — build command entries
     architecture_notes (string, optional) — architecture description prose
     test_commands (list[{command, description}], optional) — test command entries
     modules (list[{name, impl, tests, docs, status}], optional) — module status rows
-#}
<!-- project-template: claude-md v1.0.0 -->
This file provides context to Claude Code. See `.claude/` for rules and memory files.

<!-- ATLAS:START -->
# {{ project_name }}

{{ project_description }}

{% if has_instructions_dir %}
## 📋 Documentation Map

```
CLAUDE.md                                    ← Project identity (this file)
├── .github/instructions/*.instructions.md   ← Coding standards (per-technology)
└── docs/                                    ← Architecture documents and ADRs
```

Coding standards live in `.github/instructions/` — **not** in this file.
{% endif %}

## 📁 Component Inventory

| Path | Purpose | Status |
|------|---------|--------|
{% for item in inventory_items %}| `{{ item.path }}` | {{ item.purpose }} | {{ item.status }} |
{% endfor %}

## 🏗️ Build & Development

{% if prerequisites %}
### Prerequisites

{{ prerequisites }}
{% endif %}

### Commands

{% for cmd in build_commands %}```bash
{{ cmd.command }}
```
{{ cmd.description }}

{% endfor %}
{% if test_commands %}
### Testing

{% for cmd in test_commands %}```bash
{{ cmd.command }}
```
{{ cmd.description }}

{% endfor %}
{% endif %}
{% if architecture_notes %}
### Architecture

{{ architecture_notes }}
{% endif %}

{% if modules %}
## 📚 Module Status

| Module | Implementation | Testing | Documentation | Status |
|--------|---------------|---------|---------------|--------|
{% for m in modules %}| `{{ m.name }}` | {{ m.impl }} | {{ m.tests }} | {{ m.docs }} | {{ m.status }} |
{% endfor %}
{% endif %}
<!-- ATLAS:END -->
